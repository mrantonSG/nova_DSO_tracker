# Nova DSO Tracker v6.1.1
**Release Date:** 2026-06-04

## Bug Fixes
- Outlook background worker no longer crashes silently on VPS deployments — numpy scalar types (np.str_, np.int_, np.float_) returned by astropy/SIMBAD are now converted to native Python before YAML serialization; debug YAML write failure no longer blocks the main JSON cache write

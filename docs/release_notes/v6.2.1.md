# Nova DSO Tracker v6.2.1

**Release Date:** 2026-07-20

## Bug Fixes

- Type column filter negation (!) now works correctly in the dashboard - rows excluded with a leading "!" are properly hidden, and the positive-match logic no longer overrides negated filters

## Security

- Updated `bleach` 6.3.0 → 6.4.0 to patch a known vulnerability

## Internal

- Added `--filter-*` CSS custom properties (L, R, G, B, HA, OIII, SII, custom) for both light and dark themes - infrastructure for future filter-color UI
- Updated `soupsieve` 2.6 → 2.8.4
- Removed dead query from `journal_list_view` (unused code path)
- Added 4 CasaOS app store screenshots to docs/screenshots/
